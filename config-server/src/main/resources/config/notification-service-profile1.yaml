eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/

email-server-url: "http://localhost:8080/api/notification-service/push-mail"

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rapidcomment
    username: postgres
    password: password
    jpa:
      show-sql: false
      hibernate:
        ddl-auto: update

    rabbitmq:
      host: localhost
      port: "5672"
      username: "guest"
      password: "guest"
